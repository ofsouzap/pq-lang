(library
 (public_name pq_lang.lib)
 (name pq_lang)
 (modules ast parsing_errors parser lexer frontend ast_executor))

; TODO - consider not making lexer and parser public modules and instead having the frontend API just use frontend.ml[i]

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --strict --explain))
