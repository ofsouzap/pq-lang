(library
 (public_name pq_lang.lib)
 (name pq_lang)
 (libraries core)
 (modules
  utils
  vtype
  pattern
  ast
  typing
  parsing_errors
  parser
  lexer
  frontend
  ast_executor)
 (preprocess
  (pps ppx_jane)))

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --strict --explain))
