(library
 (public_name pq_lang.lib)
 (name pq_lang)
 (libraries core qcheck z3)
 (modules
  utils
  vtype
  variant_types
  varname
  pattern
  ast
  quotient_types
  custom_types
  program
  typing
  quotient_type_checking
  parsing_errors
  parser
  lexer
  frontend
  ast_executor)
 (preprocess
  (pps ppx_jane)))

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --strict --explain))
